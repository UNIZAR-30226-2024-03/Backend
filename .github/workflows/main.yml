name: Despliegue a Azure y ejecución de test

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      PRIVATE_KEY: ${{ secrets.AZURE_SECRET_KEY }}

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
     # - run: npm install
     # - run: npm run build --if-present
     # - run: npm test
      - name: Upload SSH key
        uses: actions/upload-ssh-key@v3
        with:
          ssh-private-key: ${{ secrets.AZURE_SECRET_KEY }}
          host: 'playbeat.uksouth.cloudapp.azure.com'
          username: 'azureuser'
      - name: Desplegar a Azure
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.AZURE_SECRET_KEY }}
        run: |
          ssh -o StrictHostKeyChecking=no azureuser@playbeat.uksouth.cloudapp.azure.com "cd playbeat/Backend && git pull origin main && npm install && npm restart"
         