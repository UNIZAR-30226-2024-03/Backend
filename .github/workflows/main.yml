name: Despliegue a Azure

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v2

    - name: Configurar Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '14'

    - name: Instalar dependencias
      run: npm install

    - name: Construir la aplicación
      run: npm run build  # Asegúrate de ajustar esto según tus necesidades

    - name: Desplegar a Azure
      run: |
        # Aquí colocas los comandos específicos para desplegar en tu servidor de Azure
        # Puedes utilizar Azure CLI, SSH, u otras herramientas según tu configuración

# Puedes agregar más pasos según tus necesidades, como notificaciones, pruebas, etc.
